import React from 'react';
import Layout from '../ui-kit/Layout';

const Parameters: React.FC = () => {
  return (
    <Layout>
      <h1>Welcome to the Parameters Page!</h1>
      <p>This is some content specific to the Parameters Page.</p>
    </Layout>
  );
};

export default Parameters;
